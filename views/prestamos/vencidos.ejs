<!DOCTYPE html>
<html lang="es">
<%- include('../partials/head') %>
<body>
    <div class="container-fluid">
        <div class="row flex-nowrap">
            <%- include('../partials/sidebar') %>

            <div class="col py-3">
                <div class="container">
                    <div class="d-flex justify-content-between align-items-center mb-4">
                        <h2 class="text-danger"><i class="bi bi-exclamation-triangle-fill"></i> Cartera Vencida (Morosos)</h2>
                        <a href="/prestamos" class="btn btn-outline-secondary">Volver a Préstamos</a>
                    </div>

                    <% if (vencidos.length > 0) { %>
                        <div class="alert alert-warning border-warning shadow-sm">
                            <i class="bi bi-info-circle-fill"></i> Atención: Hay <strong><%= vencidos.length %></strong> préstamos que han superado su fecha límite de pago.
                        </div>

                        <div class="card card-box p-0 overflow-hidden border-danger">
                            <div class="table-responsive">
                                <table class="table table-hover align-middle mb-0">
                                    <thead class="table-danger">
                                        <tr>
                                            <th>ID</th>
                                            <th>Cliente</th>
                                            <th>Contacto</th>
                                            <th>Deuda Total</th>
                                            <th>Fecha Límite</th>
                                            <th>Días de Retraso</th>
                                            <th>Acciones</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        <% vencidos.forEach(p => { 
                                            // Calcular días de retraso
                                            const fechaFin = new Date(p.fecha_fin);
                                            const hoy = new Date();
                                            const diffTime = Math.abs(hoy - fechaFin);
                                            const diffDays = Math.ceil(diffTime / (1000 * 60 * 60 * 24));
                                        %>
                                            <tr>
                                                <td><span class="badge bg-danger">#<%= p.id %></span></td>
                                                <td>
                                                    <div class="fw-bold"><%= p.nombre %> <%= p.apellido %></div>
                                                    <small class="text-muted">DNI: <%= p.dni %></small>
                                                </td>
                                                <td>
                                                    <% if(p.telefono) { %>
                                                        <a href="https://wa.me/<%= p.telefono %>" target="_blank" class="btn btn-sm btn-success text-white" title="Enviar WhatsApp">
                                                            <i class="bi bi-whatsapp"></i> <%= p.telefono %>
                                                        </a>
                                                    <% } else { %>
                                                        <span class="text-muted">-</span>
                                                    <% } %>
                                                </td>
                                                <td class="fw-bold text-danger fs-5">$ <%= parseFloat(p.monto_total).toFixed(2) %></td>
                                                <td class="text-danger"><%= fechaFin.toLocaleDateString() %></td>
                                                <td>
                                                    <span class="badge bg-warning text-dark">+<%= diffDays %> días</span>
                                                </td>
                                                <td>
                                                    <a href="/pagos/registrar/<%= p.id %>" class="btn btn-sm btn-primary shadow-sm">
                                                        <i class="bi bi-cash-coin"></i> Cobrar Ahora
                                                    </a>
                                                </td>
                                            </tr>
                                        <% }) %>
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    <% } else { %>
                        <div class="text-center py-5">
                            <i class="bi bi-check-circle-fill text-success" style="font-size: 4rem;"></i>
                            <h3 class="mt-3 text-success">¡Excelente!</h3>
                            <p class="text-muted">No tienes préstamos vencidos en este momento.</p>
                        </div>
                    <% } %>

                </div>
            </div>
        </div>
    </div>
<%- include('../partials/footer') %>